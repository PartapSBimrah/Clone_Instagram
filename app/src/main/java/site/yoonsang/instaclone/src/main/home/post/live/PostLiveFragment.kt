package site.yoonsang.instaclone.src.main.home.post.live

import android.os.Bundle
import android.view.View
import site.yoonsang.instaclone.R
import site.yoonsang.instaclone.config.BaseFragment
import site.yoonsang.instaclone.databinding.FragmentPostLiveBinding

class PostLiveFragment : BaseFragment<FragmentPostLiveBinding>(
    FragmentPostLiveBinding::bind,
    R.layout.fragment_post_live
) {
    override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)
    }
}